# Direct dependencies only (verified against the actual imports in
# generator.py, business_rules.py, customer_segmentation_analysis.py,
# and customer_segmentation_dashboard.py - see AUDIT_REPORT.md section
# 5.7 for why the old file, a raw pip freeze, was hard to trust).
#
# Versions below were re-verified on 2026-09-18: generator.py and
# customer_segmentation_analysis.py were both run end to end against
# them, and the dashboard was launched headless with a health check.
# All results matched the numbers in README.md exactly.
#
# Need the exact transitive versions too (e.g. for a locked deploy)?
# Run `pip freeze` in a working environment and keep that alongside
# this file instead of replacing it - a curated list and a full lock
# file serve different purposes.

numpy==2.5.3
pandas==3.0.6
scikit-learn==1.9.1
scipy==1.18.1
matplotlib==3.11.2
seaborn==0.13.2
plotly==7.1.0
streamlit==1.64.0
